Master: START-ADDRESS-STOP
SLAVE: START-ADDRESS-DATA-STOP

Code em viết trong này chỉ là dùng thêm MAX 485 kết hợp với UART chứ không dùng protocol của RS485(START-ADDRESS-FUNCTION-DATA-CRC-STOP) 
Em không biết là mình có cần phải viết theo chuẩn RS485 hay chỉ cần thực hiện được chức năng là đọc được dữ liệu liệu từ 2 module sensor
rồi gửi lên Ras thôi ?